# RNTest

Builder's React Native test app: a stock bare React Native template with a counter screen.
It exists to verify signed archive builds through the Builder CLI on GitHub Actions and TestFlight.
